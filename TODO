helpers: password adder

alternate grey between different months

WHOLE ROW CHANGES COLOR UPON HOVER

create + button to add new workouts (use direct password instead of login)

get rid of nginx and serve directly with go?

change README image when done

win: "user=postgres dbname=trails password=postgres sslmode=disable"
unix: "user=postgres dbname=trails sslmode=disable host=/run/postgresql"
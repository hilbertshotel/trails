
finish wrk.Sort() function for all types of workouts

SORTER backend function should not return http.Error - instead return the standard workouts and some error msg

separate rows of different month by color 

create + button to add new workouts (use direct password instead of login)

create a helper script to dump all workouts from db into json file

get rid of nginx and serve directly with go?

change README image when done
